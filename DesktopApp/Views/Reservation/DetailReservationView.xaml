<Window x:Class="DesktopApp.Views.Reservation.DetailReservationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopApp.Views.Reservation"
        mc:Ignorable="d"
        Title="detailReservationView" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Historial de Comunicaciones" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="icHistory">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#EEE" BorderThickness="0,0,0,1" Padding="10">
                            <StackPanel>
                                <TextBlock Text="{Binding Content}" TextWrapping="Wrap" FontSize="14"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="{Binding Author}" FontWeight="Bold" Foreground="Gray" FontSize="11"/>
                                    <TextBlock Text=" • " Foreground="Gray"/>
                                    <TextBlock Text="{Binding SentAt, StringFormat='dd/MM/yyyy HH:mm'}" Foreground="Gray" FontSize="11"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Margin="0,15,0,0">
            <TextBox x:Name="txtNuevaNota" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Tag="Escribir nueva nota..."/>
            <Button Content="Añadir Nota Manual" Click="BtnGuardarNota_Click" Margin="0,10,0,0" Padding="10"/>
        </StackPanel>
    </Grid>
</Window>
